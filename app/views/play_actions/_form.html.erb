<%= form_with(model: play_action) do |form| %>
  <% if play_action.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(play_action.errors.count, "error") %> prohibited this play_action from being saved:</h2>

      <ul>
        <% play_action.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
