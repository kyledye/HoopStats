<%= form_with(model: season_team) do |form| %>
  <% if season_team.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(season_team.errors.count, "error") %> prohibited this season_team from being saved:</h2>

      <ul>
        <% season_team.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
