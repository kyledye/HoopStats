<%= form_with(model: foul) do |form| %>
  <% if foul.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(foul.errors.count, "error") %> prohibited this foul from being saved:</h2>

      <ul>
        <% foul.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
