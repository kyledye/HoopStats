<%= form_with(model: shot_grade) do |form| %>
  <% if shot_grade.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shot_grade.errors.count, "error") %> prohibited this shot_grade from being saved:</h2>

      <ul>
        <% shot_grade.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
