<%= form_with(model: play_action_group) do |form| %>
  <% if play_action_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(play_action_group.errors.count, "error") %> prohibited this play_action_group from being saved:</h2>

      <ul>
        <% play_action_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
